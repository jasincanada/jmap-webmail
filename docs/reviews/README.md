# Review artifacts

The `jasmail-dev-os` orchestrator writes merged specialist reviews here before any release tag.

**Naming:** `YYYY-MM-DD-vX.Y.Z-review.md`

**Ship rule:** Tagging is forbidden unless the file contains `Final verdict: SHIP CLEAR: 0` from all non-skipped specialists.